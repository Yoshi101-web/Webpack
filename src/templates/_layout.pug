doctype html

block locals
  - var title = 'My Website'

html(lang ="en")
  head
    meta(charset="UTF-8")
    mata(name="viewport" content="idth=device-width, initial-scale=1.0")
    title #{title}
  body
  include _menu.pug
  block content
